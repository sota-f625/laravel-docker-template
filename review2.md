# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
<input type="hidden" name="_method" value="PUT">
上記が出力されデベロッパーツールで確認することができる

### findメソッドの引数に指定しているIDは何のIDか
URLパラメータから渡されたTodoのidカラムの値

### findメソッドで実行しているSQLは何か
SELECT * FROM "todos" WHERE "todos"."id" = $id;

### findメソッドで取得できる値は何か
パラメータから渡された$idと一致するidカラムを持つレコードを、Todoインスタンスとして取得している

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
モデルがすでにDBに存在するかどうか(existsフラグ)を基準に、
存在すればUPDATE、存在しなければINSERT実行するよう切り替えています

## Todo論理削除

### traitとclassの違いとは
- クラスの継承とは異なり1つのクラスに複数のトレイトを追加することができる
- トレイト自体はインスタンス化できない

### traitを使用するメリットとは
クラスにプロパティやメソッドを追加するための機能で、
複数のクラス間でコードを共通化・再利用することが可能になります

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
コントローラに対するルートへリクエストが来た時、そのコントローラがインスタンス化され、そのタイミングでコンストラクタが実行される

### RequestクラスからFormRequestクラスに変更した理由
バリデーションや認可処理を専用のクラスに切り出し、コントローラの責務を分離し、可読性・保守性・再利用性を高めるため

### $errorsのhasメソッドの引数・返り値は何か
- 引数は文字列(formのname属性 'content')
- 返り値は真偽値(今回のif文ではtrueの場合はエラーが出た時)

### $errorsのfirstメソッドの引数・返り値は何か
- 引数は文字列(formのname属性 'content')
- 返り値は文字列(エラーメッセージ or null)

### フレームワークとは何か
アプリケーション開発を効率化するため用意された、共通の仕組みや部品、ルールをまとめた枠組み

### MVCはどういったアーキテクチャか
開発効率を高めるために作られた構造
- Model: データベースとのやり取りやアプリケーションのルールを担当する
- View: Controllerから受け取ったデータを表示し、ユーザーに画面として見せる
- Controller: ユーザーからのリクエストを受け取り、Modelから必要なデータを取得してViewに渡す

### ORMとは何か、またLaravelが使用しているORMは何か
オブジェクト指向プログラミングとリレーショナルデータベースを繋ぐ仕組みです。テーブルのレコードをオブジェクトとして扱うことができ、SQLを意識せずにプログラムからデータの操作が可能になります。
Laravelで使用されているORMはEloquent ORMです。

### composer.json, composer.lockとは何か
- composer.json: PHPプロジェクトで使用するパッケージやそのバージョン範囲などを定義するファイル
- composer.lock: 実際にインストールされたパッケージとそのバージョンを固定するためのファイル

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
プロジェクトルートのvenderディレクトリに格納されています。

